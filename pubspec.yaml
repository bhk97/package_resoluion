name: fdr
description: Flutter Dependency Resolver — detect and resolve version conflicts across pubspec, Gradle, and CocoaPods.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  args: ^2.4.0
  yaml: ^3.1.0
  http: ^1.2.0
  pub_semver: ^2.1.0
  path: ^1.9.0
  mason_logger: ^0.3.0

dev_dependencies:
  test: ^1.25.0
  mocktail: ^1.0.0
  lints: ^4.0.0

executables:
  fdr: fdr
